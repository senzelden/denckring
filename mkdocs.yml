site_name: denckring
site_description: A library of experimental writing procedures, where the validator is the eval
site_url: https://senzelden.github.io/denckring/
repo_url: https://github.com/senzelden/denckring
repo_name: senzelden/denckring
edit_uri: ""

theme:
  name: material
  features:
    - navigation.instant
    - navigation.top
    - navigation.sections
    - content.code.copy
    - search.highlight
  palette:
    - media: "(prefers-color-scheme: light)"
      scheme: default
      toggle:
        icon: material/weather-night
        name: Switch to dark mode
    - media: "(prefers-color-scheme: dark)"
      scheme: slate
      toggle:
        icon: material/weather-sunny
        name: Switch to light mode

markdown_extensions:
  - admonition
  - tables
  - toc:
      permalink: true
  - pymdownx.highlight
  - pymdownx.superfences

plugins:
  - search
  - mkdocstrings:
      handlers:
        python:
          options:
            show_source: false
            show_root_heading: true
            docstring_section_style: table
            merge_init_into_class: true
            show_if_no_docstring: true
            show_signature_annotations: true
            separate_signature: true

nav:
  - Home: index.md
  - Gallery: gallery/index.md
  - Showcase: showcase/index.md
  - API reference:
      - Reading a report: api/index.md
      - Functions: api/functions.md
      - Types: api/types.md
      - Errors: api/errors.md
      - Language packs: api/packs.md
  - Design decisions:
      - "1. English API, multilingual content": adr/0001-english-api-multilingual-content.md
      - "2. `check` mandatory, `apply` optional": adr/0002-check-mandatory-apply-optional.md
      - "3. Catalogue in YAML, not SQLite": adr/0003-catalogue-yaml-not-sqlite.md
      - "4. Language packs as capability-declaring plugins": adr/0004-language-packs-as-capabilities.md
      - "5. Continuous `score`, not a boolean": adr/0005-continuous-score.md
      - "6. Dual licence: MIT code, CC BY catalogue": adr/0006-dual-licence.md
      - "7. One module per procedure": adr/0007-one-module-per-procedure.md
      - "8. The catalogue lives inside the package": adr/0008-catalogue-inside-package.md
      - "9. Diacritic folding is a parameter, not a policy": adr/0009-folding-is-a-parameter.md
      - "10. Language packs are discovered through an entry-point group": adr/0010-packs-via-entry-points.md
      - "11. Checkability is a property of the procedure, not of its capabilities": adr/0011-checkability.md
      - "12. Two syllable capabilities, with the weaker one as the floor": adr/0012-syllable-capabilities.md
      - "13. Linguistic data ships as separate distributions": adr/0013-data-as-a-separate-distribution.md
      - "14. Metre is a satisfiability question, not a comparison": adr/0014-metre-as-satisfiability.md
      - "15. One capability per question the lexicon is asked": adr/0015-lexicon-capabilities.md
      - "16. One data pack per language, and a collision is an error": adr/0016-one-pack-per-language.md
      - "17. A device's own data ships with its procedure": adr/0017-devices-ship-with-their-procedures.md
      - "18. Whether the author stated the rule is its own field": adr/0018-attestation.md
      - "19. Figures are alphabets read at several levels": adr/0019-figures.md
      - "20. A corpus is supplied by the reader, never by the package": adr/0020-corpora-are-not-shipped.md
      - "21. Implement the transferable part, not the artefact": adr/0021-the-transferable-part.md
      - "22. German is a built-in default, not an entry point": adr/0022-german-moves-to-a-default.md
      - "23. The German lexicon is Wikidata Lexemes": adr/0023-the-german-lexicon.md
      - "24. Apache-2.0 for the code": adr/0024-apache-two-for-the-code.md
      - "25. `apply` gets the spine `check` has": adr/0025-the-apply-spine.md
      - "26. `_produce -> list[str]` is the primitive; `apply` returns its first result": adr/0026-the-production-surface.md
      - "27. `_produce` returns `Produced`, so a candidate can carry its score": adr/0027-candidates-carry-their-scores.md
      - "28. A word list graded by commonness, because a membership oracle cannot rank": adr/0028-the-graded-lexicon.md
      - "29. Where the catalogue already offered a choice, the parameter now exists": adr/0029-the-choices-the-catalogue-promised.md
      - "30. German pronunciations come from Wiktionary": adr/0030-german-pronunciations.md
      - "31. A device is an address space": adr/0031-devices-are-address-spaces.md
      - "32. The French lexicon": adr/0032-the-french-lexicon.md
      - "33. The catalogue has two layers": adr/0033-the-layer-split.md
      - "34. French verse is counted a line at a time": adr/0034-french-prosody.md
      - "35. A letter has two readings, and `requires` says which": adr/0035-orthographic-and-phonetic-readings.md
      - "36. A leading proclitic is not a noun": adr/0036-the-elision-seam.md
      - "37. The calculator word": adr/0037-the-calculator-word.md
      - "38. German frequency, and a source whose case survives": adr/0038-german-frequency.md
      - "39. Mutation testing is a worklist, not a gate": adr/0039-mutation-testing.md
  - Contributing: contributing.md
  - Changelog: changelog.md

# The gallery is generated by scripts/build_docs.py and is not in the nav
# tree beyond its index: 145 entries would drown it. Links from the index and
# from search reach the individual pages.
not_in_nav: |
  gallery/*.md

# Working documents, not publications: a dated per-procedure snapshot, and the
# specs and plans a chapter is worked out in.
#
# `superpowers/` is here for an unusual reason and it is worth stating. It and its
# history were removed from the repository on 2026-09-06, and then the directory
# was recreated on disk as an *untracked* local scratch area — see
# `.git/info/exclude`. So it is invisible to git and visible to mkdocs, which
# publishes whatever sits under `docs/` regardless of what git thinks. Without
# this line a local spec would be published to the public site.
#
# `seed/` and `expansion_ideas/` are not listed: those were removed outright and
# not recreated, and `test_every_excluded_directory_still_exists` refuses an
# exclusion naming a directory that is not there.
#
# `stage_mockups/` is the A/B/C drafts the nine machines on the explorer's stage
# were chosen from, `options/index.html` being the chooser itself. The stage that
# was chosen is the running app under `apps/`, which mkdocs never sees; these are
# the discarded alternatives, addressed to whoever picks the design work up. They
# reached the build only because nothing links them and nothing had to: mkdocs
# publishes what is under `docs/`, so 32 unreferenced files would have gone to a
# public URL on the first tagged deploy.
exclude_docs: |
  audit/
  superpowers/
  stage_mockups/
  feedback/
  research/

strict: true
