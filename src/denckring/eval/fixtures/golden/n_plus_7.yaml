procedure: n_plus_7
lang: en
cases:
  - name: displaced-by-seven
    source: constructed example; the procedure is Jean Lescure's (1961)
    text: |-
      the catafalque satchmo on the tablespoonful
    params: { source: the cat sat on the table }
    satisfied: true
  - name: a-non-noun-changed
    source: constructed counterexample
    text: |-
      a cat sat on the table
    params: { source: the cat sat on the table }
    satisfied: false
  - name: german-unchanged-nouns-tolerated
    lang: de
    source: >-
      constructed example; computed by calling n_plus_7.check directly. Katze and
      Tisch are indexed nouns left unchanged, which the checker accepts as
      ambiguous part-of-speech rather than a violation — verified against the
      lexicon and the procedure, not hand-written.
    text: |-
      Die Katze schläft auf dem Tisch.
    params: { source: "Die Katze schläft auf dem Tisch." }
    satisfied: true
  - name: german-non-noun-changed
    lang: de
    source: >-
      constructed counterexample; computed by calling n_plus_7.check directly.
      "Die" is changed to "Eine", a non-noun substitution the checker flags.
    text: |-
      Eine Katze schläft auf dem Tisch.
    params: { source: "Die Katze schläft auf dem Tisch." }
    satisfied: false
