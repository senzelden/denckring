# mathews_algorithm

*Audited 2026-08-31. Generated by the audit pass; edited by hand where noted.*

| | |
|---|---|
| kind | `constructive` |
| family | `word` |
| checkability | `source` |
| generator | yes |
| requires | `tokens` |
| declares | en, de |
| runs in | en, de, fr |

**Verdict: see note.**

**Vacuous on a single row**: the algorithm rotates rows against each other.

## Checked against an ordinary text

- **en** — satisfied: `True`, score `1.0`, violations: none
- **de** — satisfied: `True`, score `1.0`, violations: none
- **fr** — satisfied: `True`, score `1.0`, violations: none

## Generated from it

- **en** — refused: `NoCandidateWord: mathews_algorithm: the source does not hold at least two blank-line separated texts sharing at least one word of common width — a table needs two rows and a column to rotate, so give a source with two or`
- **de** — refused: `NoCandidateWord: mathews_algorithm: the source does not hold at least two blank-line separated texts sharing at least one word of common width — a table needs two rows and a column to rotate, so give a source with two or`
- **fr** — refused: `NoCandidateWord: mathews_algorithm: the source does not hold at least two blank-line separated texts sharing at least one word of common width — a table needs two rows and a column to rotate, so give a source with two or`
