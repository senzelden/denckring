# quenina

*Audited 2026-08-31. Generated by the audit pass; edited by hand where noted.*

| | |
|---|---|
| kind | `restrictive` |
| family | `form` |
| checkability | `self` |
| generator | no |
| requires | `tokens` |
| declares | en |
| runs in | en, de, fr |

**Verdict: see note.**

**Was broken, fixed in this pass.** Unparametrised, it accepted every text: the size inference returned 1 for all input, no stanza was compared against the rotation, and the empty tally scored 1.0. The checker itself was always correct with `n` given. See the CHANGELOG entry and `infer_size`.

## Checked against an ordinary text

- **en** — satisfied: `False`, score `0.2353`, violations: `invalid_size`, `missing_line`, `missing_line`, `missing_line`
- **de** — satisfied: `False`, score `0.2353`, violations: `invalid_size`, `missing_line`, `missing_line`, `missing_line`
- **fr** — satisfied: `False`, score `0.2353`, violations: `invalid_size`, `missing_line`, `missing_line`, `missing_line`
